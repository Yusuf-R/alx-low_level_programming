#include "main.h"

/**
 * int _islower - Entry point
 * Check if alphabet is lower
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{	
		return (0);
	}
	else
	{
		return (1)
	}
	_putchar('\n');
}

